"use strict";

module.exports = function(url) {

    return {
        "content": "There is some content",
        "status": "Ok"
    }
}


function processNew(raw_collection) {
    // repoSaveNew
}

function processPreviousNotCompleted() {
    // getIDsNotCompleted
    // by default this year; can start filter by month
    // getNewActions
    // logActionsChanges
    //
}